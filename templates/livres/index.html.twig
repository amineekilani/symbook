{% extends 'base.html.twig' %}

{% block title %}Hello LivresController!{% endblock %}

{% block body %}
    <table class="table table-striped">
        <tr>
            <th>ID</th>
            <th>Titre</th>
            <th>Editeur</th>
            <th>Date d'édition</th>
            <th>Prix</th>
        </tr>
        {% for livre in livres %}
            <tr>
                <td>{{ livre.id }}</td>
                <td>{{ livre.titre }}</td>
                <td>{{ livre.editeur }}</td>
                <td>{{ livre.dateEdition|date('Y-m-d') }}</td>
                <td>{{ livre.prix }}</td>
                <td><a href="/livres/show/{{livre.id}}">Consulter</a></td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}